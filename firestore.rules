rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents to any user
    // This is for testing - in production you should tighten these rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}